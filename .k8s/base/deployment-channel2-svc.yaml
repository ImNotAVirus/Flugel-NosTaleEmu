apiVersion: v1
kind: Service
metadata:
  name: channel-service2-public
spec:
  ports:
  - name: channel-port
    port: 30502
    nodePort: 30502
    targetPort: 30502
  selector:
    app: flugel
    name: channel-service2
    tier: frontend
  type: NodePort
  # type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: channel-service2-deployment
  labels:
    app.kubernetes.io/name: channel-svc
    app.kubernetes.io/instance: flugel
    app.kubernetes.io/version: 0.1.0
    app.kubernetes.io/component: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flugel
      name: channel-service2
      tier: frontend
  template:
    metadata:
      labels:
        app: flugel
        name: channel-service2
        tier: frontend
    spec:
      containers:
      - name: channel-service2
        image: channel_service
        imagePullPolicy: Never
        ports:
        - containerPort: 30502
        envFrom:
        - configMapRef:
            name: core-config
        - configMapRef:
            name: postgres-config
        env:
        - name: WORLD_NAME
          value: "ElvenGard"
        - name: CHANNEL_MAX_PLAYERS
          value: "50"
        - name: CHANNEL_PORT
          value: "30502"
        - name: HOSTNAME
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: NODE_IP
          valueFrom:
            fieldRef:
              fieldPath: status.hostIP
